{
  "name": "tskpay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix apps/tskpay-desktop run tauri -- dev",
    "desktop:dev": "npm --prefix apps/tskpay-desktop run tauri -- dev",
    "desktop:build": "npm --prefix apps/tskpay-desktop run tauri -- build"
  }
}
